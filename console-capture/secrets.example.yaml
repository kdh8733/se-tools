# 로컬 시크릿 예시. `secrets.yaml`로 복사 후 채워라(secrets.yaml은 .gitignore됨 - 절대 커밋 금지).
# 운영에서는 이 파일 대신 Vault / AWS Secrets Manager 구현체를 써라(OPERATIONS.md 'Credentials' 참고).
# 키 우선순위: "<vendor>:<ipmi_ip>"  >  "<vendor>"  >  "default"
default:
  username: admin
  password: changeme
mock:
  username: mock
  password: mock
dell:
  username: root
  password: calvin        # iDRAC 출고 기본값 예시 - 실환경에선 반드시 교체/Vault화
lenovo:
  username: USERID
  password: PASSW0RD
hpe:
  username: Administrator
  password: changeme
supermicro:
  username: ADMIN
  password: changeme
# 특정 호스트만 다른 자격증명: "<vendor>:<ip>" 키로 오버라이드
# dell:192.0.2.20:
#   username: svc-bmc
#   password: ...
